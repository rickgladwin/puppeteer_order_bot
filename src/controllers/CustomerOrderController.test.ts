
describe('CustomerOrderController', () => {
    describe('placeOrder', () => {
        it.todo('places a valid order')
        it.todo('returns a confirmation for a valid order')
    })
    describe('validateOrder', () => {
        it.todo('validates a valid customer order')
        describe('rejects invalid orders', () => {
            it.todo('rejects an order containing an item with invalid category')
            it.todo('rejects an order containing an item with invalid subcategory')
            it.todo('rejects an order containing an item with invalid item name')
        })
    })
})
