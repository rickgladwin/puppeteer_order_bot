
describe('OrderItemController', () => {
    describe('fetchOrderItemCategories', () => {
        it.todo('returns available item categories with subcategories')
    })
    describe('saveOrderItemCategories', () => {
        it.todo('saves available item categories in a persistence layer')
    })
    describe('validateOrderItem', () => {
        it.todo('validates a customer order item')
        describe('rejects invalid order items', () => {
            it.todo('rejects an item with invalid category')
            it.todo('rejects an item with invalid subcategory')
            it.todo('rejects an item with invalid item name')
        })
    })
})
